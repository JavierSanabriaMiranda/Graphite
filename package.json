{
  "name": "graphite-root",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "front:dev": "npm run dev --prefix front",
    "android:prep": "xcopy /s /y /i tauri\\resources\\android\\res tauri\\gen\\android\\app\\src\\main\\res",
    "android:dev": "npm run android:prep && tauri android dev",
    "android:build": "npm run android:prep && tauri android build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-sql": "^2.3.2",
    "@tauri-apps/plugin-stronghold": "^2.3.1",
    "@tiptap/suggestion": "^3.20.1"
  }
}
